# react-simple-maps@3.0.0's declared peer range (^16.8.0 || 17.x || 18.x)
# doesn't include React 19 yet, though it works fine in practice. Without this,
# `npm install` fails outright with ERESOLVE on any fresh clone.
legacy-peer-deps=true
